# cad-reproduce — 工作断点

最后更新：本次会话。**这是恢复工作的入口文档。**

---

## 0. 用户的三项决定（已确认，不要再问）

1. **工作目录**：已授予 `D:\CAD作业` 读写权限（提权 `icacls` 授予 `刘心小弟\AA` Modify + (OI)(CI)）。
   本目录可正常写入。**只授了这一个目录，D 盘其余部分仍只读。**
2. **阶段二方案**：按"几何 1:1 复刻（保证与 PDF 像素级一致）+ 标注用活 DIMENSION 但数值取自源图文本"执行。
3. **阶段二完成后停下，向用户解释阶段三。**

用户另外两条长期要求：
- **默认全程不依赖 GPT 识图**，但**保留 GPT 视觉升级为可选关卡**（只在脚本判定"原图有歧义"时启用、用户可随时手动触发）。
- 上一轮继承的 Codex 识图产物要保留复用（路径见 §5）。

---

## 1. 用户对上一轮的四条批评 + 实测到的真实根因

| 用户反馈 | 真实根因 | 现状 |
|---|---|---|
| 线条颜色识别错 | 不是没识别，是**过度应用绿色**：旧代码 `or fill==绿` 条款配合字形轮廓细分，让 4514 条实体（98.3% 是数字轮廓折线）全染到绿色层，真绿线只有 75 条 | **已修**：新判据下真绿线 75 条 |
| 实心红圆用几根线冒充 | 算出的线宽超出 DXF 上限（源图 1.44pt ≈ 38mm，DXF 上限 2.11mm），于是用填充多边形近似；但闭合多段线**默认不填充**，所以看起来空心 | **已修**：改用真 HATCH + 真 CIRCLE，102 个实心红点 |
| 没实现标注功能 | 根本没试过 `DIMENSION` 实体，尺寸线/界线/数字全当普通图元画 | **已修**：25 个活 DIMENSION |
| 文字用短线拼凑 | 用的是 SHX **描边**字体（`txt.shx`），SHX 的本质就是短线拼字 | **已修**：改用 SimSun（填充字形） |

**关键结论：四条里有三条不需要绕路。** 实测证明 HATCH 实心、DIMENSION 活标注、MTEXT/TEXT 文字在
ezdxf 1.4.4 + AutoCAD 2021 上全部原生可用。

---

## 2. 阶段一：已完成并验收通过

新建 `scripts/stage1_build.py`，把四项 CAD 原生能力落地。

### 四项能力的最佳实现路径（全部实测）

| 功能 | ❌ 错误做法 | ✅ 最佳路径 |
|---|---|---|
| 实心图形 | 闭合 LWPOLYLINE（默认不填充）／圆内画线 | `h=msp.add_hatch(); h.set_solid_fill(color=aci); h.paths.add_edge_path().add_arc(center,r,0,360)` + 叠加真 `CIRCLE` |
| 标注 | 手工画尺寸线+界线+文本 | `d=msp.add_linear_dim(base,p1,p2,dimstyle=...,text=...); d.render()` |
| 多行/文字 | 多条短线拼字 | `msp.add_text(...)`（默认）／`add_mtext(...)`（需换行时）|
| 颜色 | 全部同色；用 ACI 7 当黑 | 六色**都有精确 ACI**；**黑用 250 不用 7**（7 是"黑或白随机"） |

### 验收证据（AutoCAD 出的 PDF，非库渲染）

```
page rect : 1191 x 842
fonts     : ['SimSun']          ← 无任何字体回退替换
text chars: 719
标注值    : 30 694.8 1116 624 607.2 480 600 504 480 600 504 30 694.8 1104 624
            676 920 525 769 120 432 120 432 984 1536   ← 25 个，与源图逐一对应
中文      : 姓名 指导教师 杨超帆 段文军 专业 日期 能动 2023.4 班级 比例 1904
            1:15 学号 成绩 … 东北大学冶金学院 蓄热式燃烧器安装图
实体      : DIMENSION 25 / HATCH 210 / CIRCLE 102 / TEXT 42 / LWPOLYLINE 2914 / SPLINE 196
AutoCAD 加载: 3332 实体，无代理实体
```

产物：`D:\CAD作业\jobs\burner\repro_v2.dxf`、`repro_v2.dwg`、
`acad_v2\repro_v2_export.pdf`（AutoCAD 出图）、`build_v2.json`（构建报告）

---

## 3. 阶段二：已完成

新建 `scripts/infer_dims.py`，从源图几何推断活标注。

### 源图的尺寸机制（实测，与直觉相反）

- **红色是中心线**，**绿色才是尺寸界线与尺寸线**，绿色数字轮廓是尺寸数字。
- 源图 25 个尺寸数字的**文本层是不可见的**（PDF `3 Tr`），可见数字其实是绿色轮廓。
- 尺寸链识别：绿色共线线段合并成 runs → 箭头留下的间隙切分出一段段尺寸 →
  绿色**垂直线段**是界线，其**远离尺寸线的那一端**是特征位置（决定偏移方向）→
  数字标签归属**最近的那一段**（否则一个数字会被十几个尺寸共用）。

### 推断结果

```
输入: 绿色水平段 35 / 垂直段 40 / 尺寸线 h23 v30 / 数字标签 25
输出: 25 个标注   high 16 / medium 10 / low 43（low 自动丢弃）
顶部链: 30 / 694.8 / 1116 / 624     全部正确
底部链: 30 / 694.8 / 1104 / 624     全部正确
垂直  : 676 / 920 / 525 / 769 / 120 / 432 / 984 / 1536 …
```

产物：`D:\CAD作业\jobs\burner\dims.json`

### **核心冲突（必须向用户讲清楚）**

`implied_pt = 标注值 ÷ (25.4/72)` 是该数字在纸面上**应该**占的长度。
实测每个标注的 `标注值 ÷ 实际画长` 比值是 **0.59 ～ 7.58**，完全离散。

也就是说：标题栏写 1:15，实测约 1:50.6，而 25 个标注里有的差 7 倍、有的竟接近 1:1。
**"让 CAD 自己量"和"数字与原图一致"在数学上无法同时成立。**

当前实现（按用户决定）：
- **标注由 CAD 的 DIMENSION 功能绘制**——界线、尺寸线、箭头、文字全部由 CAD 生成；
- **数值取源图文本**，保证与原图一致；
- 提供 `--associative` 开关：等图纸几何与数字自洽时，切过去由 CAD 自动测量维护。

---

## 4. 已查明并修掉的坑（全部实测）

### AutoCAD 无头调用

1. **退出码完全不可信**：脚本文件不存在也返回 0 且无输出；**关键字错误不报错而是 100% CPU 死循环**。
   必须四重判定：超时 + 杀进程树 + 输出文件体积 + 自打印标记。
2. **重定向 stdout 是 UTF-16LE**，按 UTF-8 解码会得到字符间夹空字节的乱码。
3. **`.scr` 必须 ANSI(GBK) 编码**，UTF-8 会**挂起**（不是报错）。
4. **不能用管道接 stdout**：子进程继承句柄，超时后读取会**第二次阻塞**。改为重定向到文件。
5. **`-PLOT` 的应答是位置参数**，且**一次答错就永久重问**。旧脚本在第 14 个提示答了非法的 `N`，
   靠多余 token 被第二个文件名提示吃掉才侥幸出图（**字节完全相同的 PDF，错误不可见**）。
   实测把该处改成研究建议的 `A` 反而**挂起**（180s 无输出），所以沿用可用的那套并记录分歧。
6. **`-PLOT` 会挂起，`-EXPORT` 不会**：`-EXPORT` 只问 5 个问题（格式/区域/详细配置/文件名/结束）。
7. **脚本末尾不要写 `QUIT`**：会被追问"确实要放弃修改吗"，应答没排队就永久等待。
   实测这一条让一次**已成功出图**的运行白等 3 分钟超时；去掉后 **4.2 秒**完成。

### ezdxf / DXF

8. **字体用家族名不用文件名**：`find_best_match("SimSun")` → `simsun.ttc`；
   传 `"simsun.ttc"` **解析失败并静默回退 `arial.ttf`**（无中文字形 → 豆腐块）。已在 build 里加硬失败守卫。
9. **`ezdxf.new(setup=True)` 是坑**：会建 26 个指向 `OpenSans-*.ttf`/`Liberation*.ttf` 的样式，
   AutoCAD 里没有 → 日志刷屏 `[simplex.shx] ← [OpenSans-Light.ttf]` 全部替换。**用 `setup=False`。**
10. **`add_linear_dim` 返回 `DimStyleOverride` 包装，不是实体**：实体在 `.dimension`，
    setter 在包装上。`dim.dxf.text` 会 `AttributeError`。`text="<>"` 本来就是默认值（关联标注）。
11. **`render()` 必须调用**，否则 DIMENSION 存在但无几何块、什么都看不见。
12. **p1/p2 必须是"被标注特征上的点"**，不是尺寸线端点。传错会让 CAD 测量偏移距离
    （实测 30mm 的标注渲染成 `4,1`）。
13. **`Frontend(...).draw_layout(finalize=True)` 会按等比坐标改掉图框尺寸**（9.975×7.093in → 6.75×4.8in），
    像素网格对不上。必须 `finalize=False` 并自己设 `set_size_inches`/`set_position`/`set_xlim`。
14. **BYLAYER 在渲染器里解析成 `#ffffff`**，白底上默认颜色几何全部不可见。
15. **源图与成品渲染不需要 `flipud`**：实测翻转会把墨水重叠率从 0.29 降到 0.14。已在 `compare.py` 加方向自检。
16. **容差带要用毫米不要用像素**：像素带在 150dpi 合适、300dpi 就无法通过。
17. **不要用墨水面积做门禁**：两种光栅化器抗锯齿范围不同，源图恒定多约 25%，会误杀正确图纸。

### 本项目特有

18. **"笔宽吞没"判据必须要求形状成立**：只比较半宽与笔宽，会把 2369 条 0.12–0.84pt 的
    **退化碎片**（线端、虚线段帽、剖面线小面）误判为实心，生成一堆无用 HATCH。
    加"≥3 个顶点、两个方向都有面积"后，2289 → 210，真正的 102 个红点全部保留。
19. **实心红点半径要加半个笔宽**：源图是"小圆 + 更宽的笔"，落纸尺寸 = 几何半径 + 笔宽/2。
    只按几何半径画会得到 2/3 大小（实测 1.2–2.6pt vs 源图 2.4pt）。
20. **`MTEXT` 在此校验渲染器里画成空方块**（同一字体：MTEXT 42 像素墨水 vs TEXT 372）。
    **但 AutoCAD 里未必有问题**——用 `-EXPORT` 出的 PDF 验证才是真值。默认用 TEXT。

### 校验渲染器的局限（重要）

**ezdxf 的 matplotlib 渲染器不按毫米画线宽**：实测 0.18/0.25mm→1px、0.50mm→2px、1.00mm→4px
（应为 2.1/3.0/5.9/11.8px）。**不要用它判定线宽**，线宽只能用 AutoCAD 出的图判定。

**它的 CJK 字体解析不可靠**：本作业的 DXF 在该渲染器里中文显示为空方块，
但 **AutoCAD 出的 PDF 字体是 `SimSun`、719 字符、中文完全正常**。
→ 因此 **AutoCAD `-EXPORT` 出的 PDF 是唯一权威校验**，库渲染只用于几何/颜色的快速迭代。

### 阶段三最终解法：自建忠实光栅化器（`trace.py`）—— 已闭环

**问题**：两个现成渲染器各缺一半——ezdxf/matplotlib **不按毫米画线宽**（0.5mm→2px、1.0mm→4px，
应为 5.9/11.8px），AutoCAD 忠实但**按视口裁边**。几何门禁需要"两样都占"。

**解法**：不再光栅化 DXF，而是**从数字直接画图**。几何坐标本来就是自己算出来的，
所以只需要一个光栅化器（只用 PIL）：
- 线宽**直接按毫米画**，没有上限、没有 policy、没有近似；
- **同一个光栅化器跑两遍**（源几何一遍、成品几何一遍）→ 线宽语义天然一致，
  任何差异都必然来自图纸本身，而不是两个渲染器的口径差。

**这一步是决定性的**。换尺子前后对比：

| 指标 | 库渲染器 | tracer |
|---|---|---|
| 几何一致（1px 带） | 0.7866 | **0.9774** |
| 黑 IoU | 0.913 | **0.9978** |
| 绿 IoU | 0.816 | **0.9731** |
| 颜色份额最差 | 5.2% | **0.50%** |

**过程中又踩一坑**：tracer 第一版忘了画 DIMENSION 实体对应的几何（尺寸线/界线/箭头），
于是把 13923 个绿色像素报成"源图有、成品没有"，看着像巨大几何缺陷，其实是**尺子少画了一样东西**。
补上 `Tracer.dimension()` 后未匹配像素 17233 → 8455。

**阶段三最终验收：全部通过**

| 项目 | 阈值 | 实测 | 判定 |
|---|---|---|---|
| 几何一致（tracer, 1px 带） | ≥ 97% | **97.74%** | ✅ |
| 颜色份额（每色） | ≤ 3% | 最差 **0.50%** | ✅ |
| 颜色 IoU（每色） | ≥ 0.70 | 最差 **0.9731**（绿） | ✅ |
| AutoCAD 加载无代理实体 | 必须 | 3553 实体 | ✅ |
| 字体无回退 | 必须 | PDF 字体仅 `SimSun` | ✅ |
| 中文可读 | 必须 | 719 字符，标题栏正常 | ✅ |
| 标注值与原图一致 | 25/25 | 25/25 | ✅ |
| 内容范围与源图一致 | ±0.5mm | 0..249.13 × 0..175.94 mm | ✅ |

**尺度分工（最终定型）**：
- `trace.py` + `compare.py` = **门禁**（快、确定、线宽忠实）
- `accad.py --export` = **交付验收**（真 AutoCAD 判定字体/填充/线宽；不再承担门禁）
- 两把尺子都报数，结论一致才可信

### 阶段三过程记录：几何门禁抓到并修掉的三个真回归

门禁一上线就把阶段一的成品判为 **0.6925**（此前旧生成器是 0.9085）。三个根因都修了：

| 问题 | 根因 | 修复 |
|---|---|---|
| 箭头只有一半大 | 我把箭头尺寸**凭感觉写成 2.5pt**，实测源图是 **5.4pt** | 改为实测值 5.4pt（`arrow_size_pt`） |
| **图框整个丢失**（占黑墨水 81%） | 尺寸抑制用的是**整条尺寸线的外接矩形**，顶部尺寸链横跨 171→315pt，那块矩形把图框一起吃了 | 改为**按方向做薄带**（横向标注只在垂直方向留 3pt 带），并按**图元**而非**路径**判断 |
| 路径被整条删除 | PDF 的一条 path 不是"一个形状"——图框和无关线段可能共处一条 path，用 path 的外接矩形判交叠会把整条删掉 | 逐 item 判断，只删真正落在带内的图元 |

修完：几何 **0.6925 → 0.7866**（1px 带），颜色 IoU 全面上升（黑 0.876→0.913、绿 0.664→0.816）。


**加宽容差带后的收敛曲线（实测）**：

| 容差带 | 几何一致率 |
|---|---|
| 0.12 mm (1px) | 0.7866 |
| 0.20 mm (2px) | 0.8874 |
| 0.30 mm (4px) | 0.9261 |
| 0.45 mm (5px) | 0.9323 |

**在 ~0.93 封顶**，说明剩余差异不是线宽而是别的东西。差异图显示几何基本对齐（大部分为灰=一致），
剩余集中在：尺寸文字横向偏移、以及源图红/黑/绿描边更粗（源图描边 0.48/0.96/1.44pt
在 300dpi 下应为 2/4/6px，而本渲染器实测把线宽夹在 1–4px）。

**结论（重要）**：几何门禁**对回归有效**（它抓到了 0.9085→0.6925 的真实回归并定位到三个 bug），
但它的**绝对阈值不可用**——因为库渲染器的线宽不忠实。绝对判定只能用 AutoCAD 出图。
`-EXPORT` 的局限：**会按视口裁切**（A3 纸上内容被裁边），所以不能直接做全幅像素比对。

### 阶段三验收现状

| 项目 | 阈值 | 实测 | 判定 |
|---|---|---|---|
| 颜色份额（每色） | ≤ 3% | 黑 1.86%（其余 <1.3%） | 差一点 |
| 几何一致（1px 带） | ≥ 97% | 78.7%（带 0.3mm 时 92.6%） | 未达，原因见上 |
| 颜色 IoU（每色） | ≥ 0.70 | 最差 0.816（绿） | **通过** |
| AutoCAD 加载无代理实体 | 必须 | 3553 实体，正常 | **通过** |
| 字体无回退 | 必须 | PDF 字体仅 `SimSun` | **通过** |
| 中文可读 | 必须 | 719 字符，标题栏正常 | **通过** |
| 标注值与原图一致 | 25/25 | 25/25 | **通过** |
| 内容范围与源图一致 | ±0.5mm | 0..249.13 × 0..175.94 mm，**完全一致** | **通过** |


---

## 4b. 第二轮：泛化缺陷修复（issue #1）

上面所有验收数字都来自**同一张**《蓄热式燃烧器安装图》——两次"完整重跑"是同一张图
（`jobs\burner\source.pdf` 与 `jobs\_selftest\source.pdf` SHA256 完全相同）。
把"丢什么 PDF 都能画对"当成目标之后，重新读代码找到四类**不报错但结果是错的**缺陷，
以及一条更根本的结论：

> **门禁与被测对象共模。** `trace.py` 的源侧影像不是 PDF 渲染，而是拿 `extract.json`
> 自己画的；`_reads_as_solid()` 在 `trace.py` 与 `stage1_build.py` 里是同一套判据的
> 两份拷贝。规则本身错了，两边一起错，一致率照样 0.98 PASS。
> **门禁能证明"没有漏画、没有错位"，不能证明"抽取与判读是对的"。**

### 修掉的四项（每项都有回归测试）

| 缺陷 | 根因 | 修法 | 判据 |
|---|---|---|---|
| **虚线全变实线** | `dashes` 从 `extract.py` 抽出后**全仓库无人读**；而且 PyMuPDF 给的是**字符串** `"[ 6 3 ] 0"`，按列表解析永远返回空 | 新增 `cadkit.dash_pattern`（解析字符串）+ `extract` 输出 `linetypes` 表 + 生成器定义真 DXF linetype + `trace.py` 两侧都按图案画 | 读回 DXF，group 49 元素个数与毫米长度必须等于源图 |
| **六色外颜色静默变黑** | `rgb_to_key` 用逐通道 >0.5 量化到 6 个名字：`#0000FF` 不在表里 → `None` → `stroke or fill or "black"`；`#1E90FF` 三通道过半 → 判成 **cyan** | 改为**按精确值**匹配名字，表外保留 `#RRGGBB` 写真彩色（group 420），图层按颜色命名；`--extract` 让门禁也用源图自己的调色板 | `rgb_to_key((0,0,1)) == "#0000FF"`；DXF 里出现 `true_color == 0x0000FF` |
| **颜色角色写死在代码里** | `infer()` 内部 `collect_segments(data,"green")` / `collect_arrows(data,"green")` 硬编码，SKILL.md 让用户"改 colour 参数"实际改不到；角色错了返回 0 个标注**当成功** | 颜色角色**检测**（箭头 + 被界线夹住的尺寸线，不用标签颜色）+ 证据表 + `dimensions:` 配置块 + 角色不对**退出码 2**；全部阈值移到 yaml | `--colour black` 必须退出 2 并说明检测到的角色 |
| **追绘的尺寸永远是绿色** | `trace.py` 里 `t.dimension(..., "green", ...)` 是字面量 | 从 `dims.json` 的 `dimension_colour` 读 | 红色尺寸的图纸，追绘结果里 red>0 且 green==0 |

### 过程中新踩的坑（已补进 PITFALLS 38–44）

1. **DXF 线型的第一个元素是"图案总长"**，不是第一段虚线。`pattern=[6pt,3pt]` 会让
   6pt 被当成总长、只剩一个元素。正确写法 `[总长, 划线, -间隔]`。
2. **漏掉 pt→mm 换算**：虚线长度正好差 2.83 倍，而目视完全看不出来（还是虚线）。
   这是我自己改出来的回归，被"读回 DXF 断言毫米长度"的测试当场抓住。
3. **`settings` 被同作用域里的 `s`（segment）遮蔽** → `KeyError: 'tol_collinear_pt'`。
   单字母变量在需要长期维护的函数里是真实风险。
4. **单色图纸让颜色角色检测失效**：实测燃烧器图上 `black` 拿到 157 条被界线夹住的 run，
   `green` 只有 14 条 —— 因为 25 个尺寸数字的**文本 span 是黑色**，标签颜色不能作为
   角色判据。所以检测必须用"箭头 + 被夹住的尺寸线"，并把无法定案的情形**报成 ambiguous**。
5. **给检测加"证据强度"门槛时的取舍**：一开始把"有数字标签"也算作 dimension-like，
   结果一张只有虚线 + 方框的图纸被判定"有尺寸机构"并退出 2。最终判据是
   **箭头 or 被界线夹住的尺寸线**，标签只算加分项。

### 验收

```
燃烧器（同一张参考图，改动前后必须一致）
  几何一致 0.982712 → 0.9827    PASS
  每色份额差 0.003422 → 0.0034  PASS
  每色 IoU 最差 0.8571（蓝，7 像素的抗锯齿伪影）

evals/test_regressions.py   20 项断言全部 PASS
```

**仍然没做的**（写在这里以免下次又当成功）：门禁独立性（源侧改用 PDF 真实渲染）、
任意角度尺寸（角度/半径/直径/斜向线性）、多页 PDF、以及在**第二张真实图纸**上验证。
最后一条是最重要的：本轮的证据来自合成 PDF，不是真实工程图。


---

## 5. 文件位置

### skill（通用流水线）

```
C:\Users\thirt\.dsh\skills\cad-reproduce\
├── HANDOFF.md                  ← 本文档
├── cad-reproduce.yaml          配置
└── scripts\
    ├── cadkit.py               共享库（调色板/ACI/光栅颜色分类/配置）
    ├── extract.py              阶段1 抽取（含光栅颜色直方图独立佐证）
    ├── infer_dims.py           阶段2 标注推断           ← 本次新建
    ├── stage1_build.py         生成器（四项原生能力）    ← 本次新建
    ├── build.py                旧生成器（保留参考）
    ├── render.py               同网格渲染（render_pair）
    ├── compare.py              自检门禁
    └── accad.py                AutoCAD 审计/DWG/出图/导出
```

### 作业目录

```
D:\CAD作业\
├── README.md
└── jobs\burner\
    ├── source.pdf              源图权威副本
    ├── extract.json            抽取结果（矢量 + 光栅直方图）
    ├── dims.json               标注推断结果
    ├── build_v2.json           构建报告
    ├── repro_v2.dxf / .dwg     ★ 阶段一成品
    ├── acad_v2\repro_v2_export.pdf  ★ AutoCAD 出图（权威）
    ├── cmp_v2\                 同网格渲染对
    ├── view_v2b.png            成品预览
    ├── probe\                  各项诊断图
    ├── probe_dimensions.py     尺寸机制探查
    ├── probe_chains.py         尺寸链探查
    ├── probe_arrows.py         箭头量化
    ├── diag_dimensions.py      标注冲突诊断      ← 关键
    ├── probe_stage1_issues.py  文字与填充判据诊断
    └── probe_roundtrip.py      字体 A/B 测试
```

### 上一轮遗留（保留，勿删）

| 内容 | 路径 |
|---|---|
| 旧代码（燃烧器图） | `C:\Users\thirt\cad_burner\` |
| 旧交付物 V1/V2/V3 | `C:\Users\thirt\Desktop\CAD_delivery\` |
| **Codex/GPT 识图产物（用户要求保留）** | `C:\Users\thirt\Documents\Codex\2026-09-15\referenced-chatgpt-conversation-this-is-an\outputs\已填写参数\` |
| 源图 PDF 权威路径 | `C:\Users\thirt\.dsh\attachments\v1\files\59\59ad3e0f5d89328601ce62233dafafe7e6eb696234aa35617c3b937478982609\` |
| 三份研究报告 | `C:\Users\thirt\cad-agent-research.md`、`C:\Users\thirt\.dsh\skills\cad-reproduce\` 相关、`C:\Users\thirt\Desktop\CAD_delivery\AUTOCAD_2021_HEADLESS_REFERENCE.md` |

---

## 6. 流水线调用（当前可用）

```powershell
$SK  = 'C:\Users\thirt\.dsh\skills\cad-reproduce\scripts'
$JOB = 'D:\CAD作业\jobs\<作业名>'
$PY  = 'python'; $env:PYTHONIOENCODING='utf-8'

# 阶段1 抽取
& $PY "$SK\extract.py" "$JOB\source.pdf" -o "$JOB\extract.json"
# 阶段2 标注推断
& $PY "$SK\infer_dims.py" "$JOB\extract.json" -o "$JOB\dims.json"
# 生成（四项原生能力 + 活标注）
& $PY "$SK\stage1_build.py" "$JOB\extract.json" "$JOB\dims.json" `
      -o "$JOB\repro.dxf" --json "$JOB\build.json"
# AutoCAD 验收：审计 + DWG + 权威出图
& $PY "$SK\accad.py" "$JOB\repro.dxf" -o "$JOB\acad" `
      --audit --dwg --export --json "$JOB\acad.json"
# 门禁：忠实光栅化 + 比对（权威自检）
& $PY "$SK\trace.py" "$JOB\extract.json" --dims "$JOB\dims.json" `
      --out-dir "$JOB\trace" --dpi 300
& $PY "$SK\compare.py" "$JOB\trace\trace_source.png" "$JOB\trace\trace_generated.png" `
      -o "$JOB\out" --dpi 300 --json "$JOB\cmp.json"
# 备选：库渲染（快，但线宽不忠实，只用于粗看）
& $PY "$SK\render.py" "$JOB\source.pdf" --pair --dxf "$JOB\repro.dxf" `
      --extract "$JOB\extract.json" --build-report "$JOB\build.json" `
      -o "$JOB\cmp" --dpi 300
```

---

## 7. 阶段四：已完成（skill 已可用）

```
C:\Users\thirt\.dsh\skills\cad-reproduce\
├── SKILL.md                    ← 主文件，127 行（规范上限 500，目标 25–150）
├── cad-reproduce.yaml          配置
├── HANDOFF.md                  ← 本文档（过程记录，不是 skill 内容）
├── references\
│   ├── PITFALLS.md             37 条实测坑，分五类
│   └── PIPELINE.md             每步完整调用 + 判据 + 排错对照表（21 条症状）
├── evals\evals.json            8 条触发用例（4 正 4 反）+ 8 条质量用例
└── scripts\                    9 个模块，全部语法检查通过
    ├── cadkit.py              共享库
    ├── extract.py             阶段1 抽取
    ├── infer_dims.py          阶段2 标注推断
    ├── stage1_build.py        生成（四项原生实体）
    ├── trace.py               忠实光栅化器（门禁基准）
    ├── compare.py             自检门禁
    ├── render.py              库渲染（快速迭代用）
    ├── accad.py               AutoCAD 审计/DWG/出图/导出
    └── build.py               早期生成器（保留参考）
```

**已验证**：
- skill 能被会话发现（目录已出现在 available_skills 中）并由 `skill` 工具正确加载、
  解析相对路径
- frontmatter `name` 与目录名一致，description 295 字符（规范建议 150–400）
- `evals.json` 解析正常
- 9 个脚本全部语法检查通过
- 完整流程重跑：`trace` 两图同网格（2993×2128），门禁 **0.9774 PASS**

**按官方规范落实的设计**（研究了 Anthropic Agent Skills 的编写指南）：
- 目录 bundle 而非单文件；`SKILL.md` 给判断，`scripts/` 给确定性逻辑
- description **单行**、含真实触发说法与明确的"不适用"（防抢别的任务的活）
- 正文含"何时用 / **何时不用**"章节
- 长内容外置到 `references/`，一级引用不嵌套
- `evals/` 先写——触发用例的正反两面都覆盖
- 内容原则："**好 skill 是伤疤，不是简历**"——写实际踩过的坑，不写模型已知的通识

---

## 8. 剩余项：做与不做的判断依据（已实测量化）

用户问过"这几项为什么不做"。重新量化后的结论如下。

### 已在本轮顺手修掉（成本 ≈ 常数，收益明确）

| 项 | 实测发现 | 处置 |
|---|---|---|
| 尺寸文字偏移 | 源图 25 个标注的数值文字**中心一律距尺寸线 3.25pt**（离散 3.25–3.25，是常数不是范围），即字高的 0.48 倍。旧值 `dimgap=2.75pt` 会把中心推到约 5.5pt，**偏离约 2.2pt（约自身字高的 1/4）** | 按 `dimgap + 字高/2 ≈ 3.25` 反解得 **dimgap ≈ 0.5pt**。几何一致率 **0.9774 → 0.9822** |
| `指导教师` 层归属 | 上一轮报告指出的是**旧 `build.py`** 的问题（按位置+字号判说明层）。**`stage1_build.py` 里该启发式根本不存在**——判层走 `_span_colour`：黄→NOTES_YELLOW、黑→TEXT。实测 `指导教师` 已在 `TEXT` 层 | **无需修改**；另在 `trace.py` 里发现同源隐患：`gap_pt` 硬编码 2.75 与生成器不一致，已统一为读同一份配置 |

### 判断后暂不做（附实测代价）

| 项 | 实测代价 | 不做的理由 |
|---|---|---|
| 箭头形状换成源图的细长填充片 | 模拟两种箭头模型逐像素比对：**2792 px = 总墨水的 0.94%** | 要改需定义自定义箭头块 + `dimblk`，引入代理实体风险；代价 0.94% 墨水、不影响任何阈值。**等哪个作业的判定阈值收紧到 1% 以内再做** |
| MTEXT 换行 | 未量化；但当前源图**每个 span 本来就只有一行**，TEXT 完全够用 | 换行只在"一段长文本需要折行"时才有意义，本作业无此需求；且库渲染器把 MTEXT 画成空方块，会牺牲可校验性 |
| 多页支持 | 未量化；`extract.py` 对多页**会警告并只取第 1 页** | 当前作业是单页。作业真出现多页时再做，属于**功能扩展**而非缺陷修复 |

**共同点：这三项都不是"做不到"，是"当前代价 > 收益"**，且触发条件都写得清楚（阈值收紧 / 出现长文本 / 出现多页）。

---

## 9. GPT 视觉能力：能补什么、不能补什么

用户明确要求保留 GPT 识图作为可选关卡。以下是**基于本作业实测**的判断。

### GPT 视觉**不能**补的（因为这些已经由测量解决了，而且测量更可靠）

| 事项 | 为什么不需要 GPT |
|---|---|
| 颜色分不清哪种线用什么颜色 | 六色都是**精确饱和值**（#FF0000/#00FF00/…），直接从 PDF 内容流读出，零歧义。实测颜色份额差 0.50% |
| 实心点识别 | 判据是几何的："笔宽 ≥ 形状半宽"，实测精确选出 102 个实心点，零误判 |
| 漏画/多画 | 差异图直接给出像素级位置，比"看一眼"精确得多 |
| 标注数值 | 源图数字是**文本层**，直接提取；25/25 全对 |
| 线宽 | 源图笔宽在 PDF 里是数值（0.48/0.96/1.44pt） |

一句话：**凡是能用几何或元数据判定的，都不该用视觉模型**——视觉是模糊测量，而这些是精确量。

### GPT 视觉**能**补的（需要语义判断、且测量给不出答案的）

1. **源图本身的语义歧义**（最高价值）
   本作业实测有一个具体案例：那 **50 个 `fs` 绿色轮廓**。分析报告的原话是
   "不知道它们代表什么数字，也无法判断它们是否与别处的标注重复——需要 OCR/形状识别"。
   这正是视觉模型该上场的地方：**读出它们是什么**。虽然它们已被正确渲染（所以不影响成品），
   但若某个作业里这类轮廓**不对应任何文本层**，那就只有视觉能读出数值。

2. **"这团几何是什么零件"的语义映射**
   `参数对照表.md` 里代号/Codex 已经承认过这个缺口：
   "尚未建立完整实体映射""视图间构件映射未被编号明确指认"。
   把轮廓聚类成"这是一个支承/端盖/法兰"是**语义判断**，几何测量给不出。
   价值在于：能按零件**参数化重建**，而不是仅逐段复刻。

3. **比例自相矛盾的裁决**
   实测每个标注"数值 ÷ 画长"比值离散在 0.59–7.58。**哪些标注是作者标错了**、
   哪些是量错了特征——这需要看懂图纸意图（"这个 1536 到底量的是哪两段之和"）。
   Codex 那份产物已经把这个列成待确认项，正是该交给视觉模型的题。

4. **退化区域的判读**
   扫描件、低质量 PDF、被压扁的字符——测量管线在这些地方会静默产出垃圾，
   而视觉模型能说"这里看不清"。

### 落地方式（已写进 skill 的边界章节）

**默认全程不依赖 GPT**；只有在**推断或门禁报出"原图歧义"**时才启用，且由用户决定。
触发条件就写上面第 1、3 条那两类——**看得见的歧义**（比例离散、标签无法归属、
某块几何无标注约束）。

Codex CLI 用法（本机**未安装**，需先装）：
`codex exec "提示词" -i 图片路径 -o 输出.md` —— **提示词必须在 `-i` 之前**。


