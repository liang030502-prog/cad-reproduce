---
name: cad-reproduce
description: 把 PDF 工程图 1:1 复刻成 AutoCAD 可编辑的 DXF/DWG，用真 HATCH、真 DIMENSION、真 TEXT 和精确 ACI 配色，并用自检门禁量化验证几何与颜色。当用户要"照着这张 PDF 画 CAD 图""复刻图纸""把 PDF 转成 DWG/DXF""按原图重新画一遍"，或抱怨上一版"颜色不对、实心图形是空心的、标注是手画的、文字是短线拼的"时使用。不适用于从零设计新图、三维建模、或只做 DXF 格式转换而不复刻原图。
---

# 工程图矢量化复刻

把一张 PDF 工程图复刻成**用 CAD 原生实体绘制**的可编辑图纸，并用可量化的自检证明它像原图。

## 核心原则

**不要用普通图元冒充 CAD 功能。** 用户能一眼看出差别，而且冒充出来的图不可编辑。
四项能力都有原生实体，都用原生实体：

| 要画的东西 | 用的实体 | 不要这样做 |
|---|---|---|
| 实心图形（实心点/填充区） | `HATCH` + `set_solid_fill()` | 闭合多段线（默认不填充，会空心）；圆内画线 |
| 尺寸标注 | `DIMENSION`（活标注） | 手工画尺寸线+界线+数字 |
| 文字 | `TEXT`（需换行用 `MTEXT`） | 多条短线拼字 |
| 线条颜色 | 精确 ACI 索引 | 全部同色；用 ACI 7 当黑（它是"黑或白随机"） |

**几何按原图打印尺寸 1:1 复刻。** 1 源点 = 25.4/72 mm，永远如此。
不要"恢复真实尺寸"——那样就无法与原图逐像素比对，而比对是唯一的验证手段。

## 何时用 / 何时不用

**用**：PDF 工程图 → 可编辑 CAD 图；用户要求"和原图一模一样"；需要交付 DXF/DWG。

**不用**：
- 从零设计新图（没有源图可复刻）
- 只需要格式转换（DXF↔DWG 用 `accad.py --dwg` 就够，不必走全流程）
- 源图是照片或低分辨率位图（本流程依赖 PDF 的矢量数据；位图需要先矢量化，那是另一件事）
- 用户只要一张示意图，不在意与源图一致

## 流程

全部脚本在 `scripts/`，用 `python <script>` 调用。`$JOB` 是一个作业目录。

### 0. 先确认环境

```bash
python -c "import ezdxf, pymupdf, PIL, numpy; print('ok')"
ls "D:/AutoCAD 2021/accoreconsole.exe"     # 路径按实际安装位置改
```

### 1. 抽取源图（`extract.py`）

```bash
python scripts/extract.py "$JOB/source.pdf" -o "$JOB/extract.json"
```

输出全部矢量路径、文字 span、**光栅颜色直方图**（独立佐证，不依赖路径元数据）、
以及**虚线图案表**。

看三样东西：

1. **颜色组合统计**——哪几种颜色承担什么角色；
2. **linetype 表**——源图有几种虚线、各自的划线/间隔长度。生成器按这张表定义
   真 DXF linetype，所以这里为空就说明源图全是实线；
3. **"colours outside the six named source colours"**——出现这一段说明这张图不是
   六饱和色图纸。脚本会把它们写成**真彩色**（group code 420），不再替换成黑色或
   邻近色。

### 2. 推断活标注（`infer_dims.py`）

```bash
python scripts/infer_dims.py "$JOB/extract.json" -o "$JOB/dims.json" \
    [--config cad-reproduce.yaml] [--colour green]
```

从源图**画出来的**尺寸几何反推标注意图。脚本会**自动检测**"哪种颜色承担尺寸线"
并打印证据表（箭头数、被界线夹住的尺寸线数、同色数字标签数）。

- 检测与你要的一致 → 继续；
- 检测说"无法从几何区分角色"（单色图纸常见）→ 按提示把答案写进
  `cad-reproduce.yaml` 的 `dimensions.colour`，或直接 `--colour X`；
- 你要的角色不对 → **退出码 2 并报错**，不会静默返回 0 个标注。

本流程实测过红色是中心线、**绿色才是尺寸线/界线**，与直觉相反。所有阈值（字高、
箭头签名、共线容差…）都在 yaml 的 `dimensions:` 块里，是**实测值**，换图改配置即可，
不用改代码。

看输出的置信度分布：**只保留 high/medium**，low 的丢弃（宁缺勿错）。

### 3. 生成图纸（`stage1_build.py`）

```bash
python scripts/stage1_build.py "$JOB/extract.json" "$JOB/dims.json" \
    -o "$JOB/repro.dxf" --json "$JOB/build.json"
```

关键参数（`cad-reproduce.yaml`）：字体家族名、箭头尺寸、文字高度。
**这些必须从源图实测**，不要凭感觉填——本流程实测过把箭头写成 2.5pt 而源图是 5.4pt，
结果所有箭头只有一半大。

### 4. 自检门禁（`trace.py` + `compare.py`）

```bash
python scripts/trace.py "$JOB/extract.json" --dims "$JOB/dims.json" \
    --out-dir "$JOB/trace" --dpi 300
python scripts/compare.py "$JOB/trace/trace_source.png" "$JOB/trace/trace_generated.png" \
    -o "$JOB/out" --dpi 300 --json "$JOB/cmp.json" --extract "$JOB/extract.json"
```

门禁判三项：**几何一致率 ≥97%**、**每色份额差 ≤3%**、**每色位置 IoU ≥0.70**。
不通过就**不要交付**，看差异图定位。

`--extract` 把颜色分类的调色板换成**源图自己的颜色**。不加它，源图里六色之外的
颜色会被归到最近的邻近色，颜色判据衡量的是调色板而不是图纸。

**门禁抓不到什么**：源侧影像是从 `extract.json` 自己画的，且实心判据在
`trace.py` 与 `stage1_build.py` 里是同一套规则的两份拷贝——**规则本身错了，
两边会一起错，一致率照样漂亮**。它只能证明"生成器没有漏画或错位"，不能证明
"抽取与判读是对的"。所以新图纸上至少要有一次人工/AutoCAD 出图的对照。

### 5. AutoCAD 权威验收（`accad.py`）

```bash
python scripts/accad.py "$JOB/repro.dxf" -o "$JOB/acad" --audit --dwg --export
```

审计给出 AutoCAD 实际加载的实体数与范围；出图 PDF 是字体/填充/线宽的权威判定。
**必须确认**：实体数合理、无代理实体、PDF 字体不是替换字体、内容范围与源图一致。

## 停下来问用户的时机

只在**原图本身有歧义**时停下，其余自己迭代到门禁通过：

- 某个标注的数值与它画出来的长度明显不符（比例离散）
- 某块几何没有任何标注约束，看不出它的尺寸意图
- 颜色角色无法确定（哪种颜色代表什么）
- 源图上同一数字出现在两处、无法判断归属

**不要**因为"自检没过"就停下——那是自己该修的。

## 交付物

```
$JOB/
├─ repro.dxf / repro.dwg          成品
├─ acad/repro_export.pdf          AutoCAD 出图（权威）
├─ out/cmp_difference.png         差异图（灰=一致，蓝=多余，红=缺失）
└─ build.json / cmp.json          构建与自检报告
```

## 边界与已知限制

- **原图的比例自洽程度要量出来，不要假设**。判据：每个标注的"数值 ÷ 实际画长"比值，
  看它能不能聚成簇。**实测参考图是自洽的**：24 个可用标注里 22 个落在簇内
  （≈17.9 / ≈14.9 / ≈20.7，各对应一个绘图比例或视图），只有 2 个离群。
  而另一张 1:20 的钢结构图在同一判据下是 **6 个簇、散成 31.8 倍、最大簇仅 3 个** ——
  那张图才真的没有主尺度。
  `dims.json` 的 `ratio_clusters` 直接给出簇与成员数，每个标注带 `ratio_checked`。
  离群不等于错：它可能只是另一个比例的局部详图，**但它是必须有人看一眼的那一类**。
  默认用活标注绘制、数值取源图；几何与数字确实自洽的图纸才加 `--associative`。
- **MTEXT 在本流程的校验渲染器里画成空方块**（同一字体 TEXT 正常）。默认用 TEXT；
  确实需要换行才用 `--mtext`，并用 AutoCAD 出图验证。
- **多页 PDF 只取第 1 页**（`extract.py` 会警告）。多页作业需要扩展。
- **位图源图不支持**：本流程依赖 PDF 的矢量数据。
- **尺寸只认水平/垂直的线性标注**。斜向尺寸、角度、半径/直径标注目前推断不出来。
- **DXF 的样条曲线不能带线型**：虚线圆弧会保留几何但丢掉虚线，`build.json` 里
  记在 `dashed_curve_not_dashed` 名下。
- **箭头签名是绝对尺寸，换比例会失效**（已修，但要知道原理）：箭头长度正比于该图的
  尺寸字高（实测 1:15 图 0.983、1:20 图 0.527），长宽比**不是**常数。
  配置的窗口一个都匹配不到时会**在图上测量**并记录来源（`rules.arrow_signature_source`）。
- **只有一张参考图纸被验证过**。上面每个"实测"数字都来自同一张机械安装图。
  换图纸时把它当成**起点**而不是结论，先跑 `extract.py` 看颜色角色与虚线表，再决定
  要不要改 `cad-reproduce.yaml`。

**所有判定参数都必须实测，不要凭感觉。** 本流程实测过的两个教训：
箭头尺寸写成 2.5pt 而源图是 5.4pt（所有箭头只有一半大）；
尺寸文字间距写成 2.75pt 而源图实测中心偏移 3.25pt（数字偏离约自身字高的 1/4）。

## 自检套件

```bash
python evals/test_repository.py       # 仓库本身：文档链接 / 可加载 / 能力是否真接线
python evals/test_regressions.py      # 流水线行为：四类门禁看不见的历史缺陷
python evals/test_arc_recognition.py  # 圆弧识别：真 ARC/CIRCLE 而非一串 SPLINE
```

三者全过才退出码 0。**改动脚本后至少跑前两个**；只改文档则跑第一个。

`test_repository.py` 会检查"**文档承诺的能力在代码里真的有接线**"——
例如虚线必须同时出现在 `extract`（产出图案表）、`stage1_build`（定义 linetype 并施加）、
`trace`（两侧都画虚线）三处，缺任何一处都算失败。这条判据是为了对付一类真实事故：
文档写了支持、代码里却有一段被合并时丢掉，而行为测试正好没有覆盖到那个组合。


## 什么时候该请视觉模型帮忙

**默认全程不依赖视觉模型**——凡能用几何或元数据判定的，测量都比"看一眼"可靠：
颜色（源图是精确饱和值）、实心识别（笔宽 ≥ 形状半宽）、漏画多画（差异图给像素位置）、
标注数值（源图有文本层）、线宽（PDF 里就是数值）。

**只有原图语义歧义才需要视觉模型**，典型三类：

1. **无法归属的数值**：某个数字既不对应任何文本层、又无法判断归属哪个特征
   （本流程实测遇到 50 个 `fs` 绿色轮廓，分析报告明确写"需要 OCR/形状识别"）
2. **比值离群标注的裁决**：`ratio_clusters` 报出的 `ratio_checked: false` 标注，
   哪些是作者标错了、哪些只是另一个比例的局部详图。**这需要看懂图纸意图**，
   几何给不出答案。实测钢结构图上 9 个标注里有 4 个离群（比值从 0.28 到 8.82），
   其中 `180` 标在 20.4pt 上——是标错、是另一个视图、还是我配错了跨度的两端，
   只有看图能定。把这 4 个连同它们的 bbox 一起交给视觉模型，别自己猜。
3. **零件的语义映射**："这团轮廓是个支承/端盖/法兰"——按零件参数化重建需要它

触发时**停下来问用户**，并给出具体的提示词与图片区域，不要自己猜。

更多坑见 `references/PITFALLS.md`，完整调用与排错见 `references/PIPELINE.md`。

